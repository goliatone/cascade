# Default dependency-registration configuration.
# skip_patterns: regular expressions of module paths to ignore.
skip_patterns:
  - "^github.com/goliatone/internal"
workflow: "custom-workflow.yml"
default_branch: "release"
workspaces:
  - services/api
  - tools/tasks
